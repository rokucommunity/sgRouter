<?xml version="1.0" encoding="utf-8"?>
<component name="Outlet" extends="Group">
    <interface>
        <!-- READ ONLY -->
        <field id="__isOutlet" type="boolean" value="true" />
        <!-- END READ ONLY -->
    </interface>
    <children>
        <Group id="keepAliveViewTarget" />
        <Group id="viewTarget" />
    </children>
</component>